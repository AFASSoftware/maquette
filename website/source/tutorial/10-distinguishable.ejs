---
layout: tutorial.ejs
---

<script>
  document.addEventListener('DOMContentLoaded', function () {
    var projector = maquette.createProjector();

    var tableOfContent = window.createTableOfContent();
    projector.merge(document.querySelector(".table-of-content"), tableOfContent.render);

    var navigation = window.createNavigationBar(projector);
    projector.merge(document.querySelector(".navigation-bar"), navigation.render);
  });
</script>
<div class="content-container">
  <div class="table-of-content"></div>
  <main>
    <article>
      <h1>Two rules</h1>

      <%- include('/partials/rules.html') -%>

      <h2>Assignment</h2>
      <p>
        The next assignment demonstrates what goes wrong when a non-distinguishable child node is removed and how maquette helps you fix it.
      </p>
      <p>
        In this assignment, we have two rows in our remote control.
        When you press the 'remove rotation' button the rotation row should be removed.
        Maquette however removes the wrong row and changes the first row into the second. It then crashes with an insightful error message on the browser
        console. Your task is to make sure the right thing happens. Good luck.
      </p>

    </article>
  </main>
</div>
<div class="navigation-bar"></div>
